#pragma once
#include "stivale2.h"
#include <stddef.h>
#include <stdint-gcc.h>
#include "util/string.h"

// extern int puts(const char * str);

void printk(const char * str);

// extern int printk(const char * __restrict str, ...);